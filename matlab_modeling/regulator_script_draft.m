%% regulator's work
%% T_zm
% T_zm = 358;     % collected water's temperature 343-408 K (70-135 C); 
                  % when t_o -> T_zm = 273K
                  % changes are delayed by 8h because of distance
% T_zm = 70 - 2.5 * (T_o - 6) % temperature in C

%% T_zco
% T_zco = 55 - 1.75 * T_o     % temperature in C